import {createStyles, Theme} from "@material-ui/core";

export const userFormRouteStyles = (theme: Theme) => createStyles({
    mainContainer: {
        height: '100%',
        display: 'flex',
        flex: '1 1 auto',
        alignItems: 'center',
        justifyContent: 'center',
        flexDirection: 'column',
    },
});